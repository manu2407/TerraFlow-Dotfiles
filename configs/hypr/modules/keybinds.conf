# ----------------------------------------------------- 
# TERRAFLOW FINAL KEYBINDINGS
# ----------------------------------------------------- 
$mainMod = SUPER

# --- 1. SYSTEM BASICS ---
# Open Terminal (Standard)
bind = $mainMod, Return, exec, kitty

# Close Active Window
bind = $mainMod, Q, killactive, 

# Fullscreen
bind = $mainMod, F, fullscreen,

# Universal Paste (Super + V) -> Runs our smart script
bind = $mainMod, V, exec, ~/.config/hypr/scripts/paste.sh

# Clipboard History (Super + Ctrl + V)
bind = $mainMod CTRL, V, exec, cliphist list | rofi -dmenu -p "Clipboard" | cliphist decode | wl-copy && wtype -M ctrl -k v -m ctrl

# Screenshot (PrintScreen)
bind = , Print, exec, hyprshot -m region --clipboard-only


# --- 2. MENUS & DASHBOARDS ---
# Quick Settings Dashboard (Super + S)
bind = $mainMod, S, exec, ~/.config/hypr/scripts/quick-settings.sh

# App Launcher (Super + Space)
bind = $mainMod, SPACE, exec, nwg-drawer

# Dashboard (Super + Alt + Space)
bind = $mainMod ALT, SPACE, exec, ags -t dashboard

# Power Menu (Super + Esc)
bind = $mainMod, ESCAPE, exec, wlogout

# Task Manager (Super + T)
bind = $mainMod, T, exec, kitty --class system_monitor -e btop

# File Manager (Super + E)
bind = $mainMod, E, exec, thunar


# --- 3. WINDOW RULES FOR THESE TOOLS ---
# Ensure Task Manager always floats and centers
windowrulev2 = float, class:(system_monitor)
windowrulev2 = size 70% 70%, class:(system_monitor)
windowrulev2 = center, class:(system_monitor)

# Ensure Rofi (Clipboard) looks like a floating dialog
windowrulev2 = float, class:(Rofi)
windowrulev2 = center, class:(Rofi)


# --- 4. NAVIGATION (Vim + Arrow) ---
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d


# --- 5. WORKSPACES ---
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mouse
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
